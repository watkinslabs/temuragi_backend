x firewall cli to add remove IP's
o firewall UI -> contact support (create/open)
o firewall UI -> requested URL/Reference ID in template.
x add makefile options for this.. or a main CLI that auto registers all CLI's and then creates submenus's for that cli....
x changed active to is_active
x added db init frfom cli
x added db data from cli
x add templates
x export import data
x render templates
x ad encrytpthon to passwords for connections
o encrypt more?
o password detection if incrypted broken... doesnt matter.. but is
o report init broken... sets as inactive/wrong options.... maybe js issue.. idk
o lost only shows active, 
o report cannot set to inactive/active
o need log viewer